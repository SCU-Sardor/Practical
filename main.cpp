#include <iostream>
using namespace std;

int main() {
    /*string domain;
    while (1) {
        cout << "\nDomain name: ";
        getline(cin, domain);
        domain = "nslookup" + domain;
        system(domain.c_str());
    }*///Sayt IP manzilini aniqlash



}